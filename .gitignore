# Dependencies
node_modules/

# Next.js build output
.next/
out/

# Environment variables (contains Firebase keys — never commit these)
.env.local
.env.*.local

# Firebase service account key (Admin SDK — never commit)
service-account.json
*-firebase-adminsdk-*.json
lore-*.json

# iOS app secrets (contains Firebase + TMDB API keys — never commit)
ios/Lore/Lore/Config.swift

# Misc
.DS_Store
*.pem
npm-debug.log*
